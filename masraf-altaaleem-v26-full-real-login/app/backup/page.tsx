import { redirect } from 'next/navigation'
import DashboardShell from '@/components/DashboardShell'
import { createClient } from '@/lib/supabase/server'
export default async function Backup(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/auth/login');const {data:m}=await supabase.from('memberships').select('tenant_id,role,tenants(name)').eq('user_id',user.id).maybeSingle();if(!m)redirect('/dashboard');return <DashboardShell active="/backup" tenant={(m.tenants as any)?.name||'مساحتي'}><div className="bankContent"><div className="pageTitle"><span className="badge">💾 حماية البيانات</span><h1>نسخة احتياطية</h1><p className="muted">النسخ الاحتياطية في النسخة التجارية يجب أن تنفذ على مستوى قاعدة البيانات والتخزين، وليست ملفًا محليًا في جوال المعلم.</p></div><section className="card composer"><h2>حالة النسخ</h2><div className="backupState"><span className="backupIcon">✓</span><div><b>البنية جاهزة للنسخ الاحتياطي المركزي</b><p className="muted">سيتم تفعيل جدول النسخ والاسترجاع مع مزود الاستضافة الإنتاجي. الاسترجاع يكون للمخولين فقط لتجنب الكتابة فوق بيانات صحيحة.</p></div></div><div className="notice">لا يوجد زر استرجاع وهمي هنا. عند ربط بيئة الإنتاج سيظهر آخر وقت نسخة وحالة الاسترجاع الفعلية.</div></section></div></DashboardShell>}
